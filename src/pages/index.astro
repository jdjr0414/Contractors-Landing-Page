---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getEntry, getCollection } from "astro:content";
import { site } from "../data/site";
import { breadcrumbSchema } from "../utils/breadcrumbs";

const page = await getEntry("pages", "home");
if (!page) throw new Error("Missing home content");
const canonicalUrl = new URL(page.data.canonicalPath, site.domain).toString();
const faq = page.data.faq ?? [];
const faqSchema = faq.length > 0 ? {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faq.map((item: { question: string; answer: string }) => ({
    "@type": "Question",
    name: item.question,
    acceptedAnswer: { "@type": "Answer", text: item.answer }
  }))
} : null;

const allPosts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());
const latestPosts = allPosts.slice(0, 6);

const cashFlowProblems = [
  { title: "Payroll gaps", desc: "Labor must be paid weekly or biweekly, but project draws may not arrive for 30–90 days.", href: "/how-contractors-pay-workers-before-invoices-clear", icon: "payroll" },
  { title: "Waiting on invoices", desc: "Work is done and invoices sent, but payment can take 60–90 days or more.", href: "/what-contractors-do-when-invoices-delayed", icon: "invoice" },
  { title: "Material deposits", desc: "Suppliers want payment before or upon delivery; clients pay after milestones.", href: "/how-contractors-buy-materials-before-getting-paid", icon: "materials" },
  { title: "Equipment breakdown costs", desc: "Unexpected repairs or replacements can drain reserves and stall jobs.", href: "/contractor-equipment-breakdown-funding", icon: "equipment" },
  { title: "Project startup costs", desc: "Mobilization, materials, and labor hit before the first payment arrives.", href: "/how-contractors-start-jobs-before-payment", icon: "startup" }
];

const paymentCycleSteps = [
  "Project Starts",
  "Contractor Buys Materials",
  "Labor Paid Weekly",
  "Work Completed",
  "Invoice Sent",
  "Client Pays 30–90 Days Later"
];

const fundingOptions = [
  { href: "/contractor-working-capital", title: "Contractor Working Capital", desc: "Short-term funding for payroll, materials, and operating gaps when client payments are delayed." },
  { href: "/construction-equipment-financing", title: "Construction Equipment Financing", desc: "Finance excavators, skid steers, trucks, and machinery. The equipment secures the financing." },
  { href: "/contractor-line-of-credit", title: "Contractor Line of Credit", desc: "Revolving access for recurring timing gaps. Draw when needed, repay when cash arrives." },
  { href: "/construction-business-loans", title: "Construction Business Loans", desc: "Term loans for expansion, acquisition, and larger projects." },
  { href: "/contractor-payroll-funding", title: "Contractor Payroll Funding", desc: "Bridge payroll when draws or invoices are delayed." }
];

const trustPoints = [
  "Clear explanations in plain language",
  "Guides built around real contractor cash-flow problems",
  "Practical comparisons of different financing options",
  "Educational resource first, sales pitch second"
];

const breadcrumbItems = [{ name: "Home", url: "/" }];
const breadcrumbSchemaJson = breadcrumbSchema(breadcrumbItems, new URL("/", site.domain).toString());
---
<BaseLayout title={page.data.title} description={page.data.description} canonicalUrl={canonicalUrl} ogType="website">
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchemaJson)} slot="head" />
  {faqSchema && (
    <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} slot="head" />
  )}
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "WebPage",
    name: page.data.title,
    description: page.data.description,
    url: canonicalUrl,
    abstract: page.data.quickAnswer
  })} slot="head" />
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "HowTo",
    name: "Why Contractor Cash Flow Gaps Happen",
    description: "The construction payment cycle creates timing gaps between when contractors pay expenses and when they receive client payment.",
    step: paymentCycleSteps.map((name, i) => ({ "@type": "HowToStep", position: i + 1, name }))
  })} slot="head" />
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "ItemList",
    name: "Contractor Funding Options",
    description: "Funding options for construction businesses: working capital, equipment financing, lines of credit, payroll funding, business loans.",
    numberOfItems: fundingOptions.length,
    itemListElement: fundingOptions.map((opt, i) => ({
      "@type": "ListItem",
      position: i + 1,
      item: {
        "@type": "Service",
        name: opt.title,
        description: opt.desc,
        url: new URL(opt.href, site.domain).toString()
      }
    }))
  })} slot="head" />

  <!-- 1. Hero Section -->
  <section class="hero-banner">
    <div class="hero-bg" aria-hidden="true">
      <img src="https://images.unsplash.com/photo-1766939514629-ca726d1a33ba?w=1600&q=85" srcset="https://images.unsplash.com/photo-1766939514629-ca726d1a33ba?w=800&q=85 800w, https://images.unsplash.com/photo-1766939514629-ca726d1a33ba?w=1200&q=85 1200w, https://images.unsplash.com/photo-1766939514629-ca726d1a33ba?w=1600&q=85 1600w" sizes="100vw" alt="Construction site with heavy equipment - contractor cash flow and financing" role="presentation" loading="eager" />
      <div class="hero-overlay"></div>
    </div>
    <div class="container hero-content">
      <p class="hero-eyebrow">Contractor Cash Flow Guide</p>
      <h1>Why the Timing Gap Happens—and What Contractors Do About It</h1>
      <p class="hero-subheading">Labor is paid weekly. Materials are paid upfront. Client invoices often pay 30–90 days later. Construction businesses face this timing mismatch every day.</p>
      <p class="hero-desc">This guide explains the cash flow problems contractors face and the funding options many use to bridge the gap between when money goes out and when it comes in.</p>
      <div class="hero-cta">
        <a class="btn btn-hero" href="/contractor-cash-flow-problems">Understand contractor cash flow</a>
        <a class="hero-link-secondary" href="/contractor-cash-flow-problems">See the most common contractor cash flow problems</a>
      </div>
    </div>
  </section>

  <!-- 2. Quick Answer + Definition -->
  <section class="section section-quick section-block">
    <div class="container container-narrow">
      <p class="section-eyebrow">Quick answer</p>
      <div class="quick-answer" role="complementary" aria-label="Quick answer">
        <p>{page.data.quickAnswer}</p>
      </div>
      <div class="definition-block">
        <h2>What Is Contractor Financing?</h2>
        <p>Contractor financing refers to funding options construction businesses use to manage cash flow between project payments. Because labor, materials, and equipment often need to be paid before invoices are collected, contractors sometimes use working capital, equipment financing, or business credit to bridge the gap.</p>
        <p>Construction payment terms—net-30, net-60, net-90—are standard in commercial and government work. Retainage holds a portion until project completion. Draw schedules may release funds every 30–45 days. Meanwhile, payroll is due weekly or biweekly. Materials are often paid on delivery. Mobilization costs hit at job start. The result is a structural timing mismatch: money goes out before money comes in. Contractor financing addresses that gap. It is not a sign of poor management—it is how the industry operates. Understanding the cycle helps contractors plan and choose the right solutions.</p>
      </div>
    </div>
  </section>

  <!-- 3. Common Contractor Cash Flow Problems -->
  <section class="section section-problems section-block section-alt">
    <div class="container">
      <p class="section-eyebrow">The problem</p>
      <h2 class="section-title">Common Contractor Cash Flow Problems</h2>
      <p class="section-intro">Construction businesses spend money before they receive it. Payroll, materials, and overhead hit on fixed schedules while client payments often arrive weeks or months later. These timing gaps create pressure even when jobs are profitable.</p>
      <div class="problems-grid">
        {cashFlowProblems.map((item) => (
          <a href={item.href} class="problem-card">
            <span class="problem-icon" aria-hidden="true">
              {item.icon === "payroll" && <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>}
              {item.icon === "invoice" && <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>}
              {item.icon === "materials" && <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/><polyline points="3.27 6.96 12 12.01 20.73 6.96"/><line x1="12" y1="22.08" x2="12" y2="12"/></svg>}
              {item.icon === "equipment" && <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>}
              {item.icon === "startup" && <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><line x1="12" y1="1" x2="12" y2="23"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>}
            </span>
            <h3>{item.title}</h3>
            <p>{item.desc}</p>
            <span class="problem-card-link">Read guide →</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- 4. Contractor Cash Flow Diagram -->
  <section class="section section-diagram section-block">
    <div class="container">
      <p class="section-eyebrow">How it works</p>
      <h2 class="section-title">Why Contractor Cash Flow Gaps Happen</h2>
      <div class="diagram-row">
        <div class="diagram-wrapper">
          <ol class="payment-cycle" role="list">
            {paymentCycleSteps.map((step) => (
              <li>{step}</li>
            ))}
          </ol>
          <p class="diagram-label">Cash Flow Gap</p>
        </div>
        <div class="diagram-explanation">
          <p>Construction payment cycles rarely align with expense cycles. Commercial and government projects often use net-30, net-60, or net-90 payment terms. Contractors complete work, submit invoices, and wait weeks or months for payment. Meanwhile, payroll, materials, and equipment costs are due on fixed schedules.</p>
          <p>Contractors feel pressure even when jobs are profitable. The business may be sound on paper, but timing creates real cash shortages. Understanding why this happens helps contractors plan and choose the right solutions.</p>
        </div>
      </div>
      <div class="key-takeaways-bar">
        <h3>Key Takeaways</h3>
        <ul class="takeaways-grid">
          <li>Expenses hit before revenue arrives—payroll, materials, and equipment are paid on fixed schedules.</li>
          <li>Client payment terms (net-30, net-60, net-90) create a timing gap even on profitable jobs.</li>
          <li>Working capital, equipment financing, and lines of credit help bridge the gap.</li>
          <li>Matching the funding type to the situation improves fit and cost-effectiveness.</li>
        </ul>
        <p class="takeaways-cta"><a href="/contractor-cash-flow-problems">Explore common problems →</a></p>
      </div>
    </div>
  </section>

  <!-- 5. Funding Options -->
  <section class="section section-funding section-block section-alt">
    <div class="container">
      <p class="section-eyebrow">The options</p>
      <h2 class="section-title">Funding Options Contractors Sometimes Use</h2>
      <p class="section-intro">Different funding tools solve different contractor cash-flow situations. The right fit depends on whether the issue is payroll timing, equipment needs, material purchases, or project growth.</p>
      <div class="funding-grid">
        {fundingOptions.map((c) => (
          <a href={c.href} class="card card-funding">
            <h3>{c.title}</h3>
            <p>{c.desc}</p>
            <span class="card-cta">Read guide →</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- 6. Trust / Authority Section -->
  <section class="section section-trust section-block">
    <div class="container">
      <p class="section-eyebrow">Why this resource</p>
      <h2 class="section-title">Why Contractors Use These Guides</h2>
      <ul class="trust-list">
        {trustPoints.map((point) => (
          <li>{point}</li>
        ))}
      </ul>
      <p class="trust-note">These guides are designed to help contractors understand their options before making any decision.</p>
    </div>
  </section>

  <!-- 7. Latest Guides / Blog Preview -->
  {latestPosts.length > 0 && (
    <section class="section section-latest section-block section-alt">
      <div class="container">
        <p class="section-eyebrow">From the guides</p>
        <h2 class="section-title">Latest Contractor Cash Flow Guides</h2>
        <div class="latest-grid">
          {latestPosts.map((post) => (
            <a href={post.data.canonicalPath} class="card card-latest">
              <p class="card-date">{post.data.pubDate.toLocaleDateString("en-US", { year: "numeric", month: "short", day: "numeric" })}</p>
              <h3>{post.data.title}</h3>
              <p>{post.data.excerpt}</p>
              <span class="card-cta">Read article →</span>
            </a>
          ))}
        </div>
        <p class="section-footer"><a href="/blog" class="btn-link">View all articles</a></p>
      </div>
    </section>
  )}

  <!-- 7b. Solutions at a Glance (bridge section) -->
  <section class="section section-solutions-bridge section-block">
    <div class="container">
      <p class="section-eyebrow">Quick reference</p>
      <h2 class="section-title">Problem → Solution at a Glance</h2>
      <p class="section-intro">Match your situation to the funding option that typically fits best. Each guide explains when and how to apply.</p>
      <div class="solutions-grid">
        <a href="/how-contractors-pay-workers-before-invoices-clear" class="solution-card">
          <span class="solution-problem">Payroll gaps</span>
          <span class="solution-arrow" aria-hidden="true">→</span>
          <span class="solution-option">Payroll funding or working capital</span>
        </a>
        <a href="/what-contractors-do-when-invoices-delayed" class="solution-card">
          <span class="solution-problem">Invoice delays</span>
          <span class="solution-arrow" aria-hidden="true">→</span>
          <span class="solution-option">Working capital or line of credit</span>
        </a>
        <a href="/how-contractors-buy-materials-before-getting-paid" class="solution-card">
          <span class="solution-problem">Material deposits</span>
          <span class="solution-arrow" aria-hidden="true">→</span>
          <span class="solution-option">Working capital or material financing</span>
        </a>
        <a href="/contractor-equipment-breakdown-funding" class="solution-card">
          <span class="solution-problem">Equipment breakdown</span>
          <span class="solution-arrow" aria-hidden="true">→</span>
          <span class="solution-option">Equipment financing or repair loans</span>
        </a>
        <a href="/how-contractors-start-jobs-before-payment" class="solution-card">
          <span class="solution-problem">Project startup</span>
          <span class="solution-arrow" aria-hidden="true">→</span>
          <span class="solution-option">Working capital or mobilization funding</span>
        </a>
      </div>
    </div>
  </section>

  <!-- 8. FAQ Section -->
  <section class="section section-faq section-block">
    <div class="container">
      <p class="section-eyebrow">Common questions</p>
      <h2 class="section-title">Common Questions Contractors Ask</h2>
      <div class="faq-layout">
        <dl class="faq-list">
          {faq.map((item) => (
            <>
              <dt>{item.question}</dt>
              <dd>{item.answer}</dd>
            </>
          ))}
        </dl>
        <aside class="faq-sidebar" role="complementary" aria-label="Related guides">
          <h3>Explore by Topic</h3>
          <p class="sidebar-geo">Funding solutions for construction businesses nationwide.</p>
          <ul>
            <li><a href="/contractor-cash-flow-problems">Contractor cash flow problems</a></li>
            <li><a href="/contractor-working-capital">Working capital for contractors</a></li>
            <li><a href="/construction-equipment-financing">Equipment financing</a></li>
            <li><a href="/contractor-line-of-credit">Lines of credit</a></li>
            <li><a href="/contractor-payroll-funding">Payroll funding</a></li>
            <li><a href="/construction-business-loans">Construction business loans</a></li>
            <li><a href="/all-funding-options">All funding options</a></li>
            <li><a href="/service-areas">Service areas by state</a></li>
          </ul>
          <p class="sidebar-cta"><a href="/blog">View all guides →</a></p>
          <a class="sidebar-btn" href="/explore-contractor-funding-options">Explore funding options</a>
        </aside>
      </div>
    </div>
  </section>

  <!-- 9. Final CTA Section -->
  <section class="section section-cta-final section-block">
    <div class="container">
      <p class="section-eyebrow">Next step</p>
      <h2 class="section-title">Explore Contractor Funding Options</h2>
      <p class="cta-copy">If your construction business is dealing with payroll gaps, invoice delays, material deposits, or equipment costs, reviewing available options can help you understand what may fit before making any decision.</p>
      <p class="cta-reassurance">{site.ctaReassurance}</p>
      <p class="cta-disclaimer">{site.ctaDisclaimer}</p>
      <a class="btn btn-cta" href="https://axiantpartners.com/match" rel="sponsored noopener noreferrer" target="_blank">See what contractor funding options may be available</a>
    </div>
  </section>

  <style>
    .section-block { border-top: 1px solid var(--border-light); }
    main .section { padding-top: 56px; padding-bottom: 56px; }
    .section-block:first-of-type { border-top: none; }
    .section-alt { background: var(--surface-concrete) !important; }

    .section-title { font-family: 'Oswald', sans-serif; font-size: clamp(1.8rem, 3vw, 3rem); font-weight: 700; line-height: 1.1; letter-spacing: -0.01em; color: var(--charcoal); margin: 0 0 20px 0; max-width: 720px; }
    .section-eyebrow { font-size: 0.8rem; font-weight: 600; letter-spacing: 0.12em; text-transform: uppercase; color: var(--cta); margin: 0 0 18px 0; }
    .section-intro { font-size: 1.125rem; color: var(--concrete); max-width: 75ch; margin: 0 0 36px 0; line-height: 1.75; }
    .section-footer { margin: 32px 0 0 0; }
    .container-narrow { max-width: 720px; }

    .hero-banner { position: relative; min-height: 520px; display: flex; align-items: center; width: 100%; }
    .hero-bg { position: absolute; inset: 0; }
    .hero-bg img { width: 100%; height: 100%; object-fit: cover; }
    .hero-overlay { position: absolute; inset: 0; background: linear-gradient(105deg, rgba(8,8,8,.92) 0%, rgba(13,13,13,.88) 40%, rgba(25,25,25,.82) 100%); pointer-events: none; }
    .hero-bg { pointer-events: none; }
    .hero-content { position: relative; z-index: 1; color: #fff; padding: 80px 0; width: 100%; max-width: 100%; box-sizing: border-box; }
    .hero-eyebrow { font-size: 0.85rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--cta); margin: 0 0 20px 0; }
    .hero-content h1 { font-family: 'Oswald', sans-serif; color: #fff; margin: 0 0 24px 0; font-size: clamp(2.5rem, 5vw, 4.5rem); font-weight: 600; letter-spacing: -0.02em; max-width: 720px; line-height: 1.05; }
    .hero-subheading { font-size: 1.25rem; font-weight: 600; color: #fff; margin: 0 0 18px 0; max-width: 75ch; line-height: 1.5; }
    .hero-desc { color: #e5e7eb; max-width: 75ch; margin: 0 0 28px 0; font-size: 1.125rem; line-height: 1.75; }
    .hero-cta { display: flex; flex-direction: column; gap: 20px; align-items: flex-start; }
    .btn-hero { padding: 18px 36px; font-size: 0.95rem; font-weight: 700; background: var(--cta) !important; color: #fff !important; letter-spacing: 0.06em; }
    .hero-link-secondary { color: #94a3b8; font-size: 0.95rem; text-decoration: underline; text-underline-offset: 3px; }
    .hero-link-secondary:hover { color: var(--cta); }

    .section-quick { background: var(--surface); }
    .quick-answer { background: #ffffff; border: 2px solid var(--border); border-left: 5px solid var(--cta); padding: 28px 48px; margin-bottom: 40px; border-radius: 0 6px 6px 0; box-shadow: var(--shadow-card); }
    .quick-answer p { margin: 0; font-size: 1.125rem; line-height: 1.75; color: var(--text); max-width: 75ch; }
    .definition-block { padding: 36px 0 0; border-top: 1px solid var(--border); margin-top: 0; }
    .definition-block h2 { font-family: 'Oswald', sans-serif; font-size: clamp(1.8rem, 3vw, 3rem); font-weight: 700; line-height: 1.1; margin: 0 0 18px 0; color: var(--charcoal); }
    .definition-block p { margin: 0; font-size: 1.125rem; line-height: 1.75; color: var(--text); max-width: 75ch; }

    .section-problems { background: var(--surface-concrete); }
    .problems-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 24px; margin-top: 40px; }
    .problem-card { display: flex; flex-direction: column; background: #ffffff; border: 2px solid var(--border); border-radius: 6px; padding: 24px; box-shadow: var(--shadow-card); transition: border-color .2s, box-shadow .2s; text-decoration: none; color: inherit; }
    .problem-card:hover { border-color: var(--cta); box-shadow: 0 4px 12px rgba(0,0,0,.08); }
    .problem-icon { display: flex; align-items: center; justify-content: flex-start; margin-bottom: 20px; color: var(--cta); }
    .problem-card h3 { font-family: 'Oswald', sans-serif; font-size: 1.3rem; font-weight: 700; margin: 0 0 14px 0; color: var(--charcoal); }
    .problem-card p { font-size: 1.0625rem; color: var(--concrete); margin: 0 0 20px 0; line-height: 1.7; flex: 1; max-width: none; }
    .problem-card-link { font-size: 0.95rem; font-weight: 600; color: var(--cta); }

    .section-diagram { background: var(--surface); padding-top: 48px; padding-bottom: 48px; }
    .diagram-row { display: grid; grid-template-columns: minmax(280px, 420px) 1fr; gap: 40px; align-items: start; margin-bottom: 32px; }
    .diagram-wrapper { background: #ffffff; border: 2px solid var(--border); border-radius: 6px; padding: 36px 40px; box-shadow: var(--shadow-card); }
    .payment-cycle { list-style: none; padding: 0; margin: 0; counter-reset: step; }
    .payment-cycle li { position: relative; padding: 14px 0 14px 48px; font-family: 'Oswald', sans-serif; font-size: 1.1rem; font-weight: 600; color: var(--charcoal); counter-increment: step; }
    .payment-cycle li::before { content: counter(step); position: absolute; left: 0; width: 30px; height: 30px; background: var(--cta); color: #fff; border-radius: 2px; display: flex; align-items: center; justify-content: center; font-size: 0.9rem; font-weight: 700; }
    .payment-cycle li:not(:last-child)::after { content: ""; position: absolute; left: 14px; top: 48px; bottom: -2px; width: 2px; background: linear-gradient(180deg, var(--cta) 0%, var(--border) 100%); }
    .diagram-label { font-family: 'Oswald', sans-serif; font-size: 1rem; font-weight: 600; color: var(--cta); margin: 20px 0 0 0; letter-spacing: 0.08em; }
    .diagram-explanation { min-width: 0; }
    .diagram-explanation p { font-size: 1.125rem; line-height: 1.75; color: var(--text); margin: 0 0 20px 0; max-width: 75ch; }
    .diagram-explanation p:last-child { margin-bottom: 0; }
    .key-takeaways-bar { background: var(--surface-concrete); border: 2px solid var(--border); border-radius: 6px; padding: 28px 36px; }
    .key-takeaways-bar h3 { font-family: 'Oswald', sans-serif; font-size: 1.2rem; font-weight: 700; margin: 0 0 20px 0; color: var(--charcoal); }
    .takeaways-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px 28px; list-style: none; padding: 0; margin: 0 0 20px 0; }
    .key-takeaways-bar li { font-size: 1rem; line-height: 1.6; color: var(--text); padding-left: 20px; position: relative; }
    .key-takeaways-bar li::before { content: ""; position: absolute; left: 0; top: 8px; width: 6px; height: 6px; background: var(--cta); }
    .takeaways-cta { margin: 0; }
    .takeaways-cta a { font-size: 0.95rem; font-weight: 600; color: var(--cta); }
    .takeaways-cta a:hover { text-decoration: underline; }

    .section-funding { background: var(--surface-concrete); }
    .funding-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 24px; margin-top: 40px; }
    .card-funding { display: flex; flex-direction: column; background: #ffffff; border: 2px solid var(--border); border-radius: 6px; padding: 24px; box-shadow: var(--shadow-card); text-decoration: none; color: inherit; transition: border-color .2s, box-shadow .2s; }
    .card-funding:hover { border-color: var(--cta); box-shadow: 0 4px 12px rgba(0,0,0,.08); }
    .card-funding h3 { font-family: 'Oswald', sans-serif; font-size: 1.3rem; font-weight: 700; margin: 0 0 14px 0; color: var(--charcoal); }
    .card-funding p { font-size: 1.0625rem; color: var(--concrete); margin: 0 0 20px 0; line-height: 1.7; flex: 1; max-width: none; }
    .card-funding .card-cta { font-size: 0.95rem; font-weight: 600; color: var(--cta); }

    .section-trust { background: var(--charcoal); color: #fff; border-top: 4px solid var(--cta); }
    .section-trust .section-eyebrow { color: var(--cta); }
    .section-trust .section-title { color: #fff; }
    .trust-list { list-style: none; padding: 0; margin: 0 0 28px 0; max-width: 560px; }
    .trust-list li { position: relative; padding: 14px 0 14px 36px; font-size: 1.125rem; line-height: 1.65; color: #e5e7eb; border-bottom: 1px solid rgba(255,255,255,.08); }
    .trust-list li:last-child { border-bottom: none; }
    .trust-list li::before { content: ""; position: absolute; left: 0; top: 20px; width: 6px; height: 6px; background: var(--cta); border-radius: 0; }
    .trust-note { font-size: 1.0625rem; color: #94a3b8; font-style: italic; margin: 0; max-width: 75ch; line-height: 1.7; }

    .section-latest { background: var(--surface-concrete); }
    .latest-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 24px; margin-top: 40px; }
    .card-latest { display: flex; flex-direction: column; padding: 24px; background: #ffffff; border: 2px solid var(--border); border-radius: 6px; box-shadow: var(--shadow-card); text-decoration: none; color: inherit; transition: border-color .2s, box-shadow .2s; }
    .card-latest:hover { border-color: var(--cta); box-shadow: 0 4px 12px rgba(0,0,0,.08); }
    .card-latest .card-date { font-size: 0.85rem; color: var(--concrete); margin: 0 0 12px 0; letter-spacing: 0.02em; }
    .card-latest h3 { font-family: 'Oswald', sans-serif; font-size: 1.25rem; font-weight: 700; margin: 0 0 14px 0; color: var(--charcoal); line-height: 1.3; }
    .card-latest p { font-size: 1.0625rem; color: var(--concrete); margin: 0 0 18px 0; line-height: 1.65; flex: 1; max-width: none; }
    .card-latest .card-cta { font-size: 0.95rem; font-weight: 600; color: var(--cta); }

    .section-solutions-bridge { background: var(--surface-concrete); }
    .solutions-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 20px; margin-top: 36px; }
    .solution-card { display: flex; flex-wrap: wrap; align-items: center; gap: 10px 12px; background: #ffffff; border: 2px solid var(--border); border-radius: 6px; padding: 20px 24px; text-decoration: none; color: inherit; transition: border-color .2s, box-shadow .2s; box-shadow: var(--shadow-card); }
    .solution-card:hover { border-color: var(--cta); box-shadow: 0 4px 12px rgba(0,0,0,.08); }
    .solution-problem { font-family: 'Oswald', sans-serif; font-size: 1.1rem; font-weight: 700; color: var(--charcoal); }
    .solution-arrow { font-size: 1.25rem; font-weight: 700; color: var(--cta); }
    .solution-option { font-size: 1rem; color: var(--concrete); line-height: 1.5; }

    .section-faq { background: var(--surface); }
    .faq-layout { display: grid; grid-template-columns: minmax(320px, 1fr) minmax(280px, 340px); gap: 40px; align-items: start; }
    .faq-list { margin: 0; min-width: 0; }
    .faq-list dd { max-width: none; }
    .faq-sidebar { background: var(--surface-concrete); border: 2px solid var(--border); border-radius: 6px; padding: 28px 32px; box-shadow: var(--shadow-card); position: sticky; top: 24px; }
    .faq-sidebar h3 { font-family: 'Oswald', sans-serif; font-size: 1.2rem; font-weight: 700; margin: 0 0 18px 0; color: var(--charcoal); }
    .sidebar-geo { font-size: 0.95rem; color: var(--concrete); margin: 0 0 18px 0; line-height: 1.6; }
    .faq-sidebar ul { list-style: none; padding: 0; margin: 0 0 20px 0; }
    .faq-sidebar li { margin: 0 0 12px 0; }
    .faq-sidebar li:last-child { margin-bottom: 0; }
    .faq-sidebar a { font-size: 1rem; color: var(--text); line-height: 1.5; }
    .faq-sidebar a:hover { color: var(--cta); }
    .sidebar-cta { margin: 0 0 20px 0; }
    .sidebar-cta a { font-size: 0.95rem; font-weight: 600; color: var(--cta); }
    .sidebar-cta a:hover { text-decoration: underline; }
    .sidebar-btn { display: block; text-align: center; padding: 14px 20px; font-size: 0.95rem; font-weight: 600; background: var(--cta); color: #fff; border-radius: 6px; margin-top: 8px; }
    .sidebar-btn:hover { background: var(--cta-hover); }
    .faq-list dt { font-family: 'Oswald', sans-serif; font-size: 1.25rem; font-weight: 700; color: var(--charcoal); margin: 24px 0 0 0; padding: 18px 24px; background: var(--surface-alt); border: 2px solid var(--border); border-bottom: none; border-radius: 6px 6px 0 0; }
    .faq-list dt:first-of-type { margin-top: 0; }
    .faq-list dd { margin: 0 0 0 0; font-size: 1.125rem; line-height: 1.75; color: var(--text); max-width: 75ch; padding: 16px 24px 24px; background: #ffffff; border: 2px solid var(--border); border-radius: 0 0 6px 6px; box-shadow: var(--shadow-card); }

    .section-cta-final { background: var(--charcoal); color: #fff; border-top: 4px solid var(--cta); }
    .section-cta-final .section-eyebrow { color: var(--cta); }
    .section-cta-final .section-title { color: #fff; }
    .cta-copy { font-size: 1.125rem; color: #e5e7eb; max-width: 75ch; margin: 0 0 20px 0; line-height: 1.75; }
    .cta-reassurance { font-size: 0.95rem; color: #94a3b8; font-style: italic; margin: 0 0 12px 0; }
    .section-cta-final .cta-disclaimer { font-size: 0.85rem; color: #94a3b8; font-style: italic; margin: 0 0 28px 0; }
    .btn-cta { padding: 18px 36px; font-size: 0.95rem; font-weight: 700; background: var(--cta) !important; color: #fff !important; letter-spacing: 0.06em; }

    @media (max-width: 768px) {
      .diagram-row { grid-template-columns: 1fr; }
      .takeaways-grid { grid-template-columns: 1fr; }
      .hero-banner { min-height: 440px; }
      .hero-content { padding: 56px 0; }
      .problems-grid, .funding-grid, .latest-grid { grid-template-columns: 1fr; }
      .faq-layout { grid-template-columns: 1fr; }
      .faq-sidebar { position: static; }
    }
  </style>
</BaseLayout>
